---
title: Home
layout: layouts/base.njk
eleventyExcludeFromCollections: true
permalink: index.html
---

<section class="posts">

  {%- include 'components/featuredPost.njk' -%}

  {%- set posts = collections.post | reverse -%}
  {%- for post in posts.slice(1, 4) -%}

    <div class="post">
    <a href="{{ post.url }}">
      <img
        src="{{ post.url }}uploads/{{ post.data.thumbnail }}"
        alt="{{ post.data.title }}"
        class="post-image"
        loading="lazy"
      />
      <div class="post-title">
        <h4>{{ post.data.title }}</h4>
        <small>
          <time datetime="{{ post.data.date | htmlDateString }}">{{ post.data.date | readableDate}}</time>
        </small>
      </div>
      </a>
    </div>

  {%- endfor -%}

</section>

<nav class="pageNav">
  <div class="previous-page"></div>
  <div class="next-page">
    <a href="/Page1">Next Page <svg
          class="icon"
          viewBox="0 0 24 24"
          aria-hidden="true"
          focusable="false"
        >
          <use xlink:href="/images/defs.svg#chevron_right"></use>
  </svg>
    </a>
  </div>
</nav>