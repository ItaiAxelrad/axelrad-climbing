<nav class="topNav">
  <div class="home">
    <h1>
      <a href="/">{{ metadata.name }}</a>
    </h1>
  </div>

  <div class="pages">
    {%- for entry in collections.pages -%}
      <a href="{{ entry.url | url }}" {% if page.url == entry.url %} class="active" {% endif %}>{{ entry.data.title }}</a>
    {%- endfor -%}
  </div>
  <button id="schemeBtn" class="btn">
  <svg
          class="icon"
          viewBox="0 0 24 24"
          aria-hidden="true"
          focusable="false"
        >
          <use xlink:href="/images/defs.svg#dark"></use>
  </svg>
  </button>
    <button id="rss" class="btn">
  <svg
          class="icon"
          viewBox="0 0 24 24"
          aria-hidden="true"
          focusable="false"
        >
          <use xlink:href="/images/defs.svg#rss"></use>
  </svg>
  </button>
</nav>