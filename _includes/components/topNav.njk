<nav class="topNav">
  <div class="home">
  <svg
          class="logo icon"
          viewBox="0 0 24 24"
          aria-hidden="true"
          focusable="false"
        >
          <use xlink:href="/images/defs.svg#logo"></use>
  </svg>
      <a href="/">{{ metadata.name }}</a>
  </div>

  <div class="pages">
    {%- for entry in collections.pages -%}
      <a href="{{ entry.url | url }}" {% if page.url == entry.url %} aria-current="page" {% endif %}>{{ entry.data.title }}</a>
    {%- endfor -%}
  </div>
  <button id="schemeBtn" name="color-scheme toggle" title="Toggle Dark / Light Mode" aria-label="color scheme button" class="btn">
  <svg
          class="icon"
          viewBox="0 0 24 24"
          aria-hidden="true"
          focusable="false"
        >
          <use xlink:href="/images/defs.svg#dark"></use>
  </svg>
  </button>
  <button id="rss" name="copy rss feed link to clipboard" title="copy rss feed link to clipboard" aria-label="copy rss feed link to clipboard" class="btn">
  <svg
          class="icon"
          viewBox="0 0 24 24"
          aria-hidden="true"
          focusable="false"
        >
          <use xlink:href="/images/defs.svg#rss"></use>
  </svg>
  </button>
</nav>