<nav class="topNav">
  <div class="home">
  <svg
          class="logo icon"
          viewBox="0 0 100 100"
          aria-hidden="true"
          focusable="false"
        >
          <use xlink:href="/images/defs.svg#logo"></use>
  </svg>
      <a href="/"><strong>axelrad</strong> climbing</a>
  </div>

  <div class="pages">
    {%- for entry in collections.pages -%}
      <a href="{{ entry.url | url }}" {% if page.url == entry.url %} aria-current="page" {% endif %}>{{ entry.data.title }}</a>
    {%- endfor -%}
  </div>
  {# <button id="schemeBtn" name="color-scheme toggle" title="Toggle Dark / Light Mode" aria-label="color scheme toggle button" class="btn">
    <svg
            class="icon"
            viewBox="0 0 24 24"
            aria-hidden="true"
            focusable="false"
          >
            <use xlink:href="/images/defs.svg#dark"></use>
    </svg>
  </button>
  <a id="rss" name="rss feed link" href="/feed.xml" class="btn">
  <span class="visually-hidden">RSS feed</span>
  <svg
          class="icon"
          viewBox="0 0 24 24"
          aria-hidden="true"
          focusable="false"
        >
          <use xlink:href="/images/defs.svg#rss"></use>
  </svg>
  </a> #}
</nav>